<span class="nightmare-dg-clip mono" title="@Title">@(string.IsNullOrEmpty(Text) ? "—" : Text)</span>

@code {
    [Parameter] public string? Text { get; set; }
    [Parameter] public string? Title { get; set; }
}
